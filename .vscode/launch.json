{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MessageBox",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/message.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Assemble",
            "miDebuggerPath": "gdb"
        }
    ]
}
